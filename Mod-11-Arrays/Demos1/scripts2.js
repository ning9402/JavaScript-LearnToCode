"use strict";

//declare an array called favFoods and put 4 different foods in it. (ex. Fajitas)

let favFoods = ["Chicken", "Steak", "Nachoes", "Rice"];



//use a for loop that lets i go from 0 to 3 and 
// inside the loop write
// console.log(favFoods[i]);

for (let i=0; i < 4; ++i) {
    console.log(favFoods[i]);
}